namespace OrderService.Api.Consumers;

// Stub for Task 1 (project scaffold) - filled in by Task 2 with the real
// Kafka consumer (dedup via ProcessedEventRepository, saga status handling).
public class OrderConsumer
{
}
