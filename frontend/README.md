# Frontend

React + Vite app (plain JavaScript, no TypeScript).

## Development

```bash
npm install
npm run dev      # http://localhost:4200
```

## Production build

```bash
npm run build     # outputs to dist/
npm run preview   # serve the production build locally
```
