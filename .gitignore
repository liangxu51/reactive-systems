target/
*.class
*.jar
*.war
*.ear

node_modules/
dist/
frontend/dist/

.idea/
*.iml
*.iws
out/

.vscode/
*.code-workspace

.settings/
*.project
*.classpath

# gemini-cli settings written by the PR review workflow
.gemini/
gha-creds-*.json

# local output from the Playwright MCP server (console logs, page snapshots, screenshots)
.playwright-mcp/

# Downloaded Helm chart dependency tarballs (#55/#54) - fetched via
# `helm dependency build`, not committed. Chart.lock (which pins the exact
# resolved subchart versions) IS committed, same convention as a
# package-lock.json.
k8s/helm/reactive-systems/charts/
